const Test = () => {
  return (
    <div>
      <h1>Test</h1>
    </div>
  );
};

export default Test;

// here as our test.js is inside pages => lists => test
// hence next.js will create a route called /lists/test